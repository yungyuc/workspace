#!/bin/bash

instname=$1
gcloud compute instances delete $instname
instdir=$HOME/var/run/gce/$instname
if [ -d $instdir ]; then rm -rf $instdir; fi

# vim: set et nu nobomb fenc=utf8 ft=sh ff=unix sw=2 ts=2:
